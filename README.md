# Isometric SVG Workbench

An interactive tool for creating and exporting isometric SVG shapes.

## Run Locally

**Prerequisites:** Node.js

1. Install dependencies: `npm install`
2. Run the app: `npm run dev`
